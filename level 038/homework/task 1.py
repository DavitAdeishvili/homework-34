def is_divisible(n,x,y):
    #we make a function called si _divisable and we have 3 parameters (n,x,y)
    if  n  % x == 0 and n % y == 0:
        #we say that if n / x doesnt give us anything back and n/y doesnt give us anything back them...
        return True
    #it should return True
    else:
        return False
    #othervise it shoul return False