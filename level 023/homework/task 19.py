def str_list (strings):
    for string in strings:
        if string. isupper():
            print (string. lower())
        else:
            print (string. upper())

str_list (["LUKA", "dato"])
